{
  "name": "guru-landing-page",
  "version": "1.0.0",
  "description": "Guru — Agent Testing Platform Landing Page",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}",
    "dev": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
